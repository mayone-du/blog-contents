{
  "scripts": {
    "prepare": "husky install",
    "new:article": "./bin/create_md.sh",
    "lint": "prettier --write ./meta/*.json"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.7",
    "prettier": "^2.5.1"
  }
}
